# 운영 DB 무결성 감사

- 감사 일시: 2026-08-08 (Asia/Seoul)
- 대상: Cloudflare D1 `retaste-production`
- 방식: `scripts/audit-db-integrity.sql` 읽기 전용 집계
- 개인정보·상호명·주소 출력: 없음
- 운영 데이터 변경: 없음 (`rows_written = 0`)
- 미적용 migration: 0개

## 무결성 결과

| 검사 항목 | 건수 | 판정 |
| --- | ---: | --- |
| 외래키 위반 | 0 | 정상 |
| 원천 종류·관리번호 중복 | 0 | 정상 |
| 국내 범위 밖 후보 좌표 | 0 | 정상 |
| 승인 후보의 공개 장소 연결 누락 | 0 | 정상 |
| 미승인 후보의 공개 장소 연결 | 0 | 정상 |
| 공개 장소의 대표 카테고리 누락 | 0 | 정상 |
| 장소별 대표 카테고리 중복 | 0 | 정상 |
| 비활성 카테고리 연결 | 0 | 정상 |
| 비말단 카테고리 연결 | 0 | 정상 |
| 활성 제외와 승인 상태 충돌 | 0 | 정상 |
| 성공 AI 결과의 카테고리 누락 | 0 | 정상 |

## 운영 현황

| 항목 | 건수 |
| --- | ---: |
| 영업 중·검수 대기 후보 | 576 |
| 주소 누락 후보 | 0 |
| 좌표 누락 후보 | 15 |
| 승인된 공공데이터 후보 | 229 |
| 공개 장소 | 255 |
| 활성 체인 제외 | 25 |
| 활성 유흥업종 제외 | 0 |
| 활성 관리자 예외 | 29 |
| 과거 AI 실패 기록 | 340 |

좌표가 없는 15건은 데이터 무결성 오류가 아니라 관리자 화면의 `승인 불가·예외` 대상이다. 공개 장소 수가 승인 공공데이터 후보 수보다 26건 많은 것은 공공데이터 승인 흐름 이전 또는 별도 등록으로 만들어진 장소가 있기 때문이며, 공개 장소의 대표 카테고리와 참조 무결성은 모두 정상이다.

## 과거 AI 카테고리 호환성

현재 활성 카테고리와 직접 일치하지 않는 과거 성공 AI 실행 기록은 102건이다.

| 과거 slug | 건수 | 현재 slug |
| --- | ---: | --- |
| `ramen` | 44 | `ramen-detail` |
| `donkatsu` | 34 | `donkatsu-detail` |
| `bakery` | 14 | `bakery-detail` |
| `gukbap` | 10 | `gukbap-detail` |

이 값들은 현재 카테고리 개편 전에 저장된 이력이며 장소나 카테고리 외래키를 깨뜨리지 않는다. 현재 AI 출력은 활성 slug만 검증하므로 신규 실행에서 같은 값이 저장되지는 않는다. 감사 이력을 보존하기 위해 이번 작업에서는 과거 실행 기록을 변경하지 않는다.

## 결론

- 외래키, 중복 원천키, 좌표 범위, 승인-장소 연결, 장소-카테고리 연결, 제외 상태의 구조적 문제는 발견되지 않았다.
- 좌표 누락 15건은 승인 불가 전용 탭에서 보완해야 한다.
- 과거 AI 성공 기록 102건은 구형 slug 이력으로, 운영 데이터 표시나 승인 연결의 구조적 오류는 아니다.
- 운영 DB 수정이나 신규 migration은 필요하지 않다.
